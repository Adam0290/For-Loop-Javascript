// For Loop //

for (let count = 0; count < arr.length ; count++) {  // initialization the counting Variable >>>>> let count = 0; //
console.log(arr[count]);  // Decide the condition >>>>> count < arr.length //
} // Range of a For Loop >>>>> Begins at the 0 and ends with the length >>>>>  0; count < arr.length

// Formula of a For Loop >>>> For (initialization: condition; update ): //